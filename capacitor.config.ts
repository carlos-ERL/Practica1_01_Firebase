import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prac03Firebase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
